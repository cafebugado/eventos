{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "middleware.ts": {
      "runtime": "@vercel/edge"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
