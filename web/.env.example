# ===========================================
# CONFIGURAÇÃO DO SUPABASE
# ===========================================
# ATENÇÃO: Nunca commite o arquivo .env com valores reais!
# Copie este arquivo para .env.local e preencha com suas credenciais:
#   cp .env.example .env.local

# URL do seu projeto Supabase (público — usado no browser e no servidor)
# Encontre em: Supabase Dashboard > Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co

# Chave pública (anon key) do Supabase (público — usado no browser e no servidor)
# Encontre em: Supabase Dashboard > Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key-aqui

# ===========================================
# SENTRY (Error Tracking - Opcional)
# ===========================================
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# ===========================================
# GITHUB (Dashboard Admin - Dados do Repositório)
# ===========================================
# Token pessoal do GitHub — agora fica só no servidor (sem prefixo público),
# já que a busca de dados do repositório passa a rodar em Server Component/Route Handler.
# Crie em: https://github.com/settings/tokens (escopo: public_repo)
# Sem token: limite de 60 req/hora (API pública)
# Com token: limite de 5000 req/hora
# GITHUB_TOKEN=ghp_seu_token_aqui
#
# Repositório frontend (padrão: cafebugado/agendas_eventos)
# GITHUB_REPO=cafebugado/agendas_eventos
#
# Repositório backend
# GITHUB_REPO_BACKEND=cafebugado/backendEventos
