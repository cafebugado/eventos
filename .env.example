# ===========================================
# API DE EVENTOS
# ===========================================
# ATENÇÃO: Nunca commite o arquivo .env com valores reais!
# Copie este arquivo para .env.local e preencha com suas credenciais:
#   cp .env.example .env.local

# URL base da nova API pública de eventos (NestJS + Prisma, somente-leitura,
# repositório D:\backendeventos-public-api — ver SPRINT.md). Sem API key: os
# endpoints consumidos por este app são todos GET públicos. Se omitida, cai
# no default de produção (https://v3.api.eventoscafebugado.cafebugado.com.br)
# — só precisa declarar pra apontar pra um backend local (ex.:
# http://localhost:3000, se as portas não colidirem com o próprio frontend).
# NEXT_PUBLIC_API_BASE_URL=https://v3.api.eventoscafebugado.cafebugado.com.br

# URL pública do site (usada para resolver URLs absolutas de metadata: og:image,
# canonical, sitemap). Default (se omitida): https://eventos.cafebugado.com.br
# NEXT_PUBLIC_SITE_URL=https://eventos.cafebugado.com.br

# ===========================================
# SENTRY (Error Tracking - Opcional)
# ===========================================
# Mesmo projeto/DSN do app antigo (era VITE_SENTRY_DSN lá) — só ativa em
# produção (NODE_ENV=production), fica sem-op em dev mesmo se preenchida.
# NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

